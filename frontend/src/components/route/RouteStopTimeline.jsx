import { MapPin } from "lucide-react";
import { Card } from "../common/Card";

export function RouteStopTimeline({ stops, selectedStop, onSelect }) {
  return <Card className="p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[var(--primary-ink)]">The journey</p><h2 className="mt-1 text-xl font-bold text-[var(--foreground)]">Route Stops</h2></div><span className="text-sm text-[var(--muted)]">{stops.length} stops</span></div>{stops.length ? <ol className="mt-6">{stops.map((stop, index) => <li key={stop._id || stop.id || `${stop.stopName}-${index}`} className="relative flex gap-4 pb-6 last:pb-0"><div className="relative flex w-7 shrink-0 justify-center"><span className={`z-10 grid h-7 w-7 place-items-center rounded-full border-2 text-xs font-bold ${selectedStop?._id === stop._id ? "border-[var(--primary)] bg-[var(--primary)] text-[var(--primary-contrast)]" : "border-[var(--primary-border)] bg-white text-[var(--primary-ink)]"}`}>{index + 1}</span>{index < stops.length - 1 && <span className="absolute top-7 h-full w-px bg-[var(--primary-border)]" />}</div><button type="button" onClick={() => onSelect(stop)} className="-mt-1 min-w-0 flex-1 rounded-xl p-2 text-left transition hover:bg-[var(--primary-soft)] focus:outline-none focus:ring-2 focus:ring-[var(--primary-soft-strong)]"><span className="block font-semibold text-[var(--foreground)]">{stop.stopName || stop.name}</span><span className="mt-1 flex items-center gap-1 text-xs text-[var(--muted)]"><MapPin size={13} /> {stop.stopOrder != null ? "Stop " + stop.stopOrder : "Route stop"}</span></button></li>)}</ol> : <p className="mt-6 text-sm text-[var(--muted)]">No stops are available for this route yet.</p>}</Card>;
}
